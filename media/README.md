# Kujo course launch image

Latest composition correction: removed the blocks, pedestal, and dotted backdrop. The K mark stands alone, centered on the right. Visually verified the uncluttered composition and preserved tagline. This supersedes the stacked-logo artwork.

Revision: the user requested Departure Mono and the exact tagline `Clarity. Context. Control.`. The updated raster uses a browser-rendered specimen of `assets/fonts/DepartureMono-Regular.woff2` as the typography reference. Visually verified the corrected tagline and lighter letterforms. Typography is image-generated from that specimen, not embedded font text. This correction supersedes the previous `WRITE. RUN. PROVE.` poster tagline. The website itself was not changed.

`kujo-course-launch.png` is a 1536 × 1024 social-media poster created on 2026-09-07 with built-in image generation. It follows the earlier Rust and TypeScript course launch posters, using monochrome artwork and the official Kujo K mark as a visual reference.

References:

- `typescript.robertdevore.com/media/typescript-course-launch.png`
- `rust.robertdevore.com/assets/rust-course-launch.png`
- This repository's `assets/kujo-logomark.svg`, sourced from `https://kujolang.ai/assets/images/kujo-logomark.svg` and rasterized on white for generation.

Visually checked the headline, 40-lesson count, tagline, domain, and recognizable K mark. PNG dimensions verified with `sips`. This is a separate promotional asset; the website's existing social metadata image remains unchanged. No social-media post was published.

## Pending memory handoff

Scope: `robertdevore/kujo.robertdevore.com`, branch `main`, social launch artwork. Intended storage: Strata `Agent Notes`. Exact deduplication search `kujo-course-launch.png social image` returned no matches. Saving this handoff failed with HTTP 500; nothing was stored and retrieval verification remains pending. Retry from the Strata repository using `npm run strata -- --agent --json agent summary --file /Users/robertdevore/2026/kujo.robertdevore.com/media/README.md --project 'Agent Notes'`. Retrieval cues: `kujo-course-launch.png`, `Kujo monochrome course launch poster`. SignalBox: no captures warranted.
