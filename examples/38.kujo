let passed := 2 == len(["a", "b"])
let receipt := {"schema": "course.run/v1", "runtime": "1.3.1", "attempts": 1, "evaluated": passed, "verdict": "PASS"}
assert_equal(receipt["evaluated"], true)
print(to_json(receipt))
