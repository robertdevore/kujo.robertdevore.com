# Course showcase

Generated with Howl 1.1.0, source revision 5470f928c5b563599778bbdf8ea0671211d3a265. `howl.json` references the already runtime-verified `examples/02.kujo`; Howl itself does not execute examples. No social content is posted.

Regenerate with `HOWL_BIN=/path/to/howl/bin/howl bash scripts/showcase.sh`. Compare `kujo-course.svg`, `.html`, `.md`, and `index.html` with Git. The social SVG embeds the licensed Departure Mono WOFF2 and a background derived from the official Kujo SVG mark. `assets/social.png` is the reviewed 1200×630 raster; use a browser-capable SVG renderer that supports embedded WOFF2. macOS sips discarded the font and misrendered the filter in testing, so it is unsuitable for this asset.
